{% extends 'base.html.twig' %}

{% block title %}{% trans %}Company details{% endtrans %}{% endblock %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}

    <div class="container" id="invoice_form">
        <div class="card">
            <div class="card-header">{% trans %}Object{% endtrans %}</div>
            <div class="card-body">
                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}
